package org.example;
//commented out so that main actually runs for now so I can test the smaller parts

/*public class Driver {

    public static void Driver(int playerCount) {
        int Array[] playerOrder = new Array[4];
        int playerOne = flipDice();
        for (int i = 0; i > playerCount - 1; i++) {
            int playerTwo = flipDice();
            if (playerOne > playerTwo) {
                break;
                }
            else if (player Two > playerOne) {



            int playerOne = flipDice();
            int playerTwo = flipDice();

        new LadderAndSnake();
    }



}*/
